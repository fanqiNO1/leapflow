"""LeapSpace — the self-evolving app environment and evaluation harness.

Ships with the leapflow distribution (``src/leapspace``, importable via the
editable install); heavier dependencies live in the root ``pyproject.toml``
under the ``leapspace`` dependency group (``make space-sync``).
"""
