"""LeapBench — benchmark harness for LeapFlow.

Not packaged or published: this package is imported by sibling entry
scripts via the directory-as-project convention (the script's directory,
``benchmarks/leapbench``, is ``sys.path[0]``). Dependencies are declared
in the root ``pyproject.toml`` under the ``leapbench`` dependency group
(``make bench-sync``).
"""
